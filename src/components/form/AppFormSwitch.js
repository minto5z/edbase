import React from 'react';
import {StyleSheet} from 'react-native';

const AppFormSwitch = (props) => {
    return (
        <React.Fragment style={styles.container}>
            <p>AppFormSwitch</p>
        </React.Fragment>
    );
};

const styles = StyleSheet.create({
    // styles goes here
});

export default AppFormSwitch;
