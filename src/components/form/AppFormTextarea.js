import React from 'react';
import {StyleSheet} from 'react-native';

const AppFormTextarea = (props) => {
    return (
        <React.Fragment style={styles.container}>
            <p>AppFormTextarea</p>
        </React.Fragment>
    );
};

const styles = StyleSheet.create({
    // styles goes here
});

export default AppFormTextarea;
